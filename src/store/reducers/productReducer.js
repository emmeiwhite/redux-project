import { SET_LOADING, GET_PRODUCTS } from "./../actions/actions";

const initialState = {
  name: "productReducer",
};

const productReducer = (state = initialState, action) => {
  return state;
};

export default productReducer;
